{
  "name": "prosjekt-2026",
  "version": "2.1.1",
  "private": true,
  "description": "Offline personal training coach. No production dependencies.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check.cjs",
    "build": "npm test && npm run check && node scripts/build.cjs"
  },
  "devDependencies": { "jsdom": "26.1.0" },
  "engines": { "node": ">=22" }
}
